# Tests

Test fixtures for automated tests (future).

Golden import fixtures and expected diagnostics will be added here alongside the v0.1 test suite according to [RFC-0004](../RFC/RFC-0004.md). They will pair with the representative inputs under `examples/` without defining a mandatory Markdown template. User collection data is **not** stored in this repository.
